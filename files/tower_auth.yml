---
tower_hostname: "{{ tower_hostname_secret }}"
tower_username: "{{ tower_username_secret }}"
tower_passname: "{{ tower_password_secret }}"
tower_validate_certs: "{{ tower_validate_certs }}"
validate_certs: "{{ validate_certs }}"
gitlab_username: "{{ gitlab_username_secret }}"
gitlab_password: "{{ gitlab_password_secret }}"